import { Cat } from "./cat.entity";
import { Diary } from "./diary.entity";
import { Auth } from "./auth.entity";
const entities = [Cat,Diary,Auth];

export {Cat,Diary,Auth };
export default entities;